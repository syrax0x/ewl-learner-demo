# English with Laura — Learner App Demo

Interactive demo of the English with Laura learner app, embedded via `<iframe>` into the
marketing site after the "IELTS, TOEFL & SAT preparation" section.

`index.html` is the design team's standalone export plus a small iframe height-reporter
(postMessage to the parent). Renders client-side (React + Babel from unpkg at runtime).
